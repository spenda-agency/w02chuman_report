# 出力Markdownのテンプレート

出力先: `/home/vagrant/vagrant_data/15_中萬/15-03_作成データ/<YYYYMM>/comment_draft_<YYYYMM>.md`

## 構成

```markdown
# 中萬学院 <YYYY>年<MM>月分 コメントドラフト

（生成日: <YYYY-MM-DD>／集計元: aggregated_<YYYYMM>.json）

## p.2 全体結果、過去同月との比較
<コメント文（3-4文、comment_patterns.mdの型に沿う）>

根拠数値: 当月CV=<n>件, CPA=¥<n>（前年同月比<±n>円）, PMAX CV=<n>件 CPA=¥<n> ...

## p.3 媒体/デバイス/検索ディスプレイ別結果
<コメント文>

根拠数値: ...

（以下、p.4〜p.14まで同じ形式で続ける。その月に存在しないスライド・データが薄い項目は
「対象データなし」と明記し、無理に埋めない）
```

## 書式ルール

- 見出しは`## p.<番号> <スライドタイトル>`で統一（`slide_layout.md`の見出しと一致させる）
- コメント文は`comment_patterns.md`の型（結果の要約→変動要因→今後の対応方針、の3文構成が基本）
- 「根拠数値」行は、コメント文中で言及した数字を列挙するだけの簡易版（`chuman-ad-report`の
  `numbers_ledger.csv`のような厳密な台帳は作らない。人間が検算しやすい最低限の情報でよい）
- データソースに存在しない値（例: クリエイティブ別CVが記録されていない等）は、コメント文でも
  断定表現を避け、「根拠数値」行に「※データソースに記録なし」と明記する
- 表・グラフ・画像は一切含めない（テキストのみ）
